package com.enable.pos.repositories;

import com.enable.pos.models.SupplierHistory;
import org.springframework.data.repository.CrudRepository;

public interface SupplierHistoryRepository extends CrudRepository<SupplierHistory, Long> {
}
